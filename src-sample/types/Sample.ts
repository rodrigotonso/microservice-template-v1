/**
 * @packageDocumentation
 * @module Types
 * Has the types for sample.
 */
export type SampleType = {
  name: string;
  age: number;
  someText: string;
};

export type PropertyType = {
  key: string;
  value: string;
};
